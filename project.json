{
  "name": "mcp-debugger",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/vscode-mcp-debugger/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run compile",
        "cwd": "packages/vscode-mcp-debugger"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'Skipping VS Code extension tests - not yet implemented'",
        "cwd": "packages/vscode-mcp-debugger"
      }
    }
  },
  "tags": ["vscode-extension"],
  "implicitDependencies": []
}
